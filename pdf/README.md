# PDF 폴더 안내

현재는 초안이며 추후 수정 가능합니다.

이 폴더는 나중에 MSDS PDF 파일을 저장하기 위한 자리입니다.

현재 1차 프로토타입에는 실제 회사 MSDS PDF 파일을 넣지 않습니다.

화면에서는 샘플 데이터의 `fileName` 값을 기준으로 아래와 같은 경로만 준비합니다.

```text
/pdf/2차이형제(S6) 2024.pdf
```

실제 운영 단계에서 PDF 파일을 넣을 때는 엑셀의 `파일명` 컬럼과 PDF 파일명이 일치하도록 관리합니다.
